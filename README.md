# findmy

Natural language search across your local files. Runs entirely on your machine — no cloud, no API keys.

```
findmy index C:\Users\you
findmy search "that script that uses COM"
findmy status
```
